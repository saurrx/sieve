{
  "name": "sieve",
  "version": "1.0.0",
  "description": "Demand Authenticity Engine for AI Agent Commerce",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "build": "cd dashboard/app && npm install && npm run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "score": "node scripts/scoring-engine.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
